// In the new Flutter plugin format, this file is intentionally minimal.
// Plugin classpath is managed via settings.gradle pluginManagement block.

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
